# Production requirements - API only (no GPU, no dev tools)

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0

# LLM & Embeddings (API-based, no local models)
openai>=1.0.0
pinecone>=3.0.0

# Reranker - CPU only version of torch
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers>=2.2.0

# Utilities
python-dotenv>=1.0.0
numpy
