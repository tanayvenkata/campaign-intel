# Production requirements - API only (no GPU, no local models)
# Uses OpenAI API for embeddings (fast) instead of local BGE-M3 (slow on CPU)

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0

# LLM & Embeddings (API-based)
openai>=1.0.0
pinecone>=3.0.0

# Utilities
python-dotenv>=1.0.0
numpy
cachetools>=5.0.0
